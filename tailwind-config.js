// tailwind-config.js
tailwind.config = {
    theme: {
        extend: {
            fontFamily: {
                display: ['Cinzel', 'serif'], // High-end architectural serif
                body: ['Montserrat', 'sans-serif'], // Geometric, structural body text
            },
            colors: {
                terracotta: '#C17767', // A warm, sophisticated accent color mimicking clay/brick
            }
        }
    }
}
